package Accounts;

public class CheckingAccount extends Account {
    public CheckingAccount(String clientId, String clientName, double balance) {
        super(clientId, clientName, balance);
    }
}
