package Accounts;

public class BusinessAccount extends Account {
    public BusinessAccount(String clientId, String clientName, double balance) {
        super(clientId, clientName, balance);
    }
}
